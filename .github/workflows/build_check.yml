name: Build check
on:
  workflow_dispatch: {}
  # No push/pull_request trigger: this calls build.yml, a full desktop (Win/Linux/
  # macOS) Cemu build unrelated to the iOS port and expensive to run on every push.

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit
